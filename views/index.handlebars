<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" rel="home" href="#" title="New York Times Scraper">
        <img style="max-width:200px; margin-top: -4px;"
             src="/assets/images/nytimeslogo.png">
    </a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="/">Home <span class="sr-only">(current)</span></a></li>
        <li class="active"><a href="/about">About <span class="sr-only"></span></a></li>
        <form action="/savedarticles" method="GET" class="navbar-form pull-right">
              <button type="submit" class="btn btn-primary btn-xs scrape">SAVED ARTICLES</button>
            </form>
       <form action="/scrape" method="POST" class="navbar-form pull-right">
              <button type="submit" class="btn btn-warning btn-xs scrape">SCRAPE ARTICLES</button>
            </form>
      </ul>
      </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
    <!--End navigation-->
    <!-- Main container -->

      <div class="jumbotron text-center">
	      <div class="caption">
		      <img src="/assets/images/start.png">
	      </div>
  </div>
    
      <div class="row">
        <div class="col-md-12">
          {{#each articles}}
          <div class="panel panel-primary article">
            <div class="panel-heading row">
              <div class="col-xs-9">
                <h3 class="panel-title pull-left">{{this.title}}</h3>
              </div>
              <div class="col-xs-3">
                <form action="/save" method="POST">
                  <input id="article-title" type="hidden" name="title" value="{{this.title}}">
                  
                  <input id="article-link" type="hidden" name="link" value={{this.link}}><strong>
                  <button type="submit" id="save" class="btn btn-warning pull-right">SAVE ARTICLE</button>
                </form>
              </div>
            </div>
            <div class="row">
              <div class="col-xs-12>">
                <div class="panel-body">
                    <a target="_blank" href={{this.link}}>
                        <p>{{this.link}}</p>
                    </a>
                </div>
              </div>
            </div>
          </div>
          {{/each}}
        </div>
      </div>
    </div>
    